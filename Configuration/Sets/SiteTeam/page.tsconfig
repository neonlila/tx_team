mod.wizards.newContentElement.wizardItems.plugins {
    elements {
        txteam_teamlist {
            iconIdentifier = content-plugin
            title = Team List
            description = Displays team members with filter options
            tt_content_defValues {
                CType = list
                list_type = txteam_teamlist
            }
        }
    }
    show := addToList(txteam_teamlist)
}